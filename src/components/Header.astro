---
import Logo from './Logo.astro';
import { url } from '../lib/url';
import { site } from '../site';

const links = [
  { href: '/learn/', label: 'Units' },
  { href: '/practice/', label: 'Practice' },
  { href: '/graph-lab/', label: 'Graph lab' },
  { href: '/glossary/', label: 'Glossary' },
  { href: '/pricing/', label: 'Pricing' },
];
const here = Astro.url.pathname;
const isActive = (href: string) => here.startsWith(url(href));
---
<header class="site-header">
  <div class="wrap bar">
    <a class="brand" href={url('/')} aria-label={`${site.name} home`}>
      <Logo />
      <span class="wordmark">Tutor<span>Econ</span></span>
    </a>

    <nav id="site-nav" class="nav" aria-label="Main">
      <ul>
        {links.map((l) => (
          <li>
            <a href={url(l.href)} aria-current={isActive(l.href) ? 'page' : undefined}>{l.label}</a>
          </li>
        ))}
      </ul>
    </nav>

    <div class="end">
      <a class="stats" href={url('/account/')} data-stats hidden aria-label="Your streak and XP">
        <span class="stat stat-streak" title="Day streak">
          <svg viewBox="0 0 24 24" width="20" height="20" aria-hidden="true"><path d="M12 2c.6 3.2-1.2 5-2.6 6.6C8 10.2 7 11.7 7 14a5 5 0 0 0 10 0c0-1.7-.6-3-1.4-4.1-.3 1.4-1 2.3-2 2.8.4-2.8-.2-7.2-1.6-10.7Z" fill="#ff7a1a" /><path d="M12 22a3 3 0 0 1-3-3c0-1.6 1-2.6 2-3.6.2 1 .8 1.6 1.6 1.9.1-.9.3-1.9.9-2.7.9 1 1.5 2.3 1.5 4.4a3 3 0 0 1-3 3Z" fill="#ffc53d" /></svg>
          <span data-streak>0</span>
        </span>
        <span class="stat stat-xp" title="XP earned">
          <svg viewBox="0 0 24 24" width="20" height="20" aria-hidden="true"><path d="M13.5 2 4 13.5h6.5L9.5 22 20 9.5h-6.8L13.5 2Z" fill="#ffc53d" stroke="#e0a000" stroke-width="1.2" stroke-linejoin="round" /></svg>
          <span data-xp>0</span>
        </span>
      </a>
      <button class="icon-btn theme-btn" type="button" data-theme-toggle aria-label="Switch to dark theme">
        <svg class="icon-moon" viewBox="0 0 24 24" width="20" height="20" aria-hidden="true"><path d="M20 14.5A8 8 0 0 1 9.5 4a8 8 0 1 0 10.5 10.5Z" fill="none" stroke="currentColor" stroke-width="2" stroke-linejoin="round" /></svg>
        <svg class="icon-sun" viewBox="0 0 24 24" width="20" height="20" aria-hidden="true"><circle cx="12" cy="12" r="4" fill="none" stroke="currentColor" stroke-width="2" /><path d="M12 2.5v2.5M12 19v2.5M2.5 12H5M19 12h2.5M5.3 5.3l1.8 1.8M16.9 16.9l1.8 1.8M5.3 18.7l1.8-1.8M16.9 7.1l1.8-1.8" stroke="currentColor" stroke-width="2" stroke-linecap="round" /></svg>
      </button>
      <a class="btn btn-small start" href={url('/learn/foundations/')}>Start learning</a>
      <button class="icon-btn menu-btn" type="button" aria-expanded="false" aria-controls="site-nav" aria-label="Menu">
        <svg viewBox="0 0 24 24" width="22" height="22" aria-hidden="true"><path d="M4 7h16M4 12h16M4 17h16" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" /></svg>
      </button>
    </div>
  </div>
</header>

<script>
  import { currentTheme, store } from '../scripts/store';

  const menuBtn = document.querySelector<HTMLButtonElement>('.menu-btn');
  const nav = document.getElementById('site-nav');
  menuBtn?.addEventListener('click', () => {
    const open = menuBtn.getAttribute('aria-expanded') === 'true';
    menuBtn.setAttribute('aria-expanded', String(!open));
    nav?.classList.toggle('is-open', !open);
  });
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && nav?.classList.contains('is-open')) {
      nav.classList.remove('is-open');
      menuBtn?.setAttribute('aria-expanded', 'false');
      menuBtn?.focus();
    }
  });

  const themeBtn = document.querySelector<HTMLButtonElement>('[data-theme-toggle]');
  const syncThemeBtn = () => {
    const next = currentTheme() === 'dark' ? 'light' : 'dark';
    themeBtn?.setAttribute('aria-label', `Switch to ${next} theme`);
    themeBtn?.setAttribute('data-current', currentTheme());
  };
  themeBtn?.addEventListener('click', () => {
    store.setTheme(currentTheme() === 'dark' ? 'light' : 'dark');
    syncThemeBtn();
    window.dispatchEvent(new CustomEvent('tutorecon:theme'));
  });
  syncThemeBtn();

  // Streak and XP, only once the reader has earned some.
  const stats = document.querySelector<HTMLAnchorElement>('[data-stats]');
  const syncStats = () => {
    const data = store.get();
    const streak = store.currentStreak();
    if (!stats) return;
    stats.hidden = data.xp <= 0;
    stats.querySelector('[data-streak]')!.textContent = String(streak);
    stats.querySelector('[data-xp]')!.textContent = String(data.xp);
    stats.setAttribute('aria-label', `${streak} day streak, ${data.xp} XP. Open your progress.`);
    stats.classList.toggle('is-cold', streak === 0);
  };
  window.addEventListener('tutorecon:change', syncStats);
  syncStats();
</script>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 40;
    background: color-mix(in srgb, var(--bg) 88%, transparent);
    backdrop-filter: saturate(1.6) blur(14px);
    -webkit-backdrop-filter: saturate(1.6) blur(14px);
    border-bottom: 2px solid var(--line);
  }
  .bar {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    min-height: 70px;
  }
  .brand {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    text-decoration: none;
    color: var(--ink);
    flex: none;
  }
  .wordmark {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 1.4rem;
    letter-spacing: -0.035em;
  }
  .wordmark span {
    color: var(--brand);
  }
  .nav {
    flex: 1;
  }
  .nav ul {
    display: flex;
    gap: 0.15rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }
  .nav a {
    display: inline-flex;
    align-items: center;
    min-height: 42px;
    padding: 0.4rem 0.8rem;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 700;
    color: var(--ink-2);
    transition: background-color 0.15s ease, color 0.15s ease;
  }
  .nav a:hover {
    color: var(--ink);
    background: var(--bg-2);
  }
  .nav a[aria-current='page'] {
    color: var(--brand);
    background: var(--brand-tint);
  }
  .end {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    margin-left: auto;
  }
  .stats {
    display: inline-flex;
    align-items: center;
    gap: 0.2rem;
    padding: 0.25rem 0.4rem;
    border-radius: 12px;
    text-decoration: none;
    border: 2px solid var(--line);
  }
  .stats:hover {
    background: var(--bg-2);
  }
  .stat {
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    padding: 0.1rem 0.35rem;
    font-weight: 800;
    font-size: 0.95rem;
  }
  .stat-streak {
    color: var(--orange-text);
  }
  .is-cold .stat-streak {
    color: var(--ink-3);
    filter: grayscale(1);
  }
  .icon-btn {
    display: inline-grid;
    place-items: center;
    width: 44px;
    height: 44px;
    border-radius: 12px;
    border: 2px solid var(--line);
    background: var(--card);
    color: var(--ink-2);
    cursor: pointer;
    box-shadow: 0 3px 0 var(--line);
    transition: transform 0.08s ease, box-shadow 0.08s ease;
  }
  .icon-btn:hover {
    color: var(--ink);
  }
  .icon-btn:active {
    transform: translateY(3px);
    box-shadow: 0 0 0 var(--line);
  }
  .start {
    margin-bottom: 3px;
  }
  .icon-sun {
    display: none;
  }
  .theme-btn[data-current='dark'] .icon-sun {
    display: block;
  }
  .theme-btn[data-current='dark'] .icon-moon {
    display: none;
  }
  .menu-btn {
    display: none;
  }
  @media (max-width: 960px) {
    .menu-btn {
      display: inline-grid;
    }
    .nav {
      display: none;
      position: absolute;
      left: 0;
      right: 0;
      top: 100%;
      padding: 0.75rem var(--gutter) 1.25rem;
      background: var(--bg);
      border-bottom: 2px solid var(--line);
      box-shadow: var(--shadow);
    }
    .nav.is-open {
      display: block;
    }
    .nav ul {
      flex-direction: column;
    }
    .nav a {
      min-height: 50px;
      font-size: 1.08rem;
      width: 100%;
    }
  }
  @media (max-width: 560px) {
    .start {
      display: none;
    }
    .wordmark {
      font-size: 1.25rem;
    }
    .bar {
      gap: 0.75rem;
    }
  }
</style>
