---
import { url } from '../lib/url';
import { site } from '../site';
import Logo from './Logo.astro';

const groups = [
  {
    title: 'Study',
    links: [
      ['/learn/', 'All units'],
      ['/practice/', 'Practice questions'],
      ['/graph-lab/', 'Graph lab'],
      ['/glossary/', 'Glossary'],
      ['/mock-exam/', 'Mock exam (Pro)'],
      ['/review/', 'Review sheets (Pro)'],
    ],
  },
  {
    title: 'Site',
    links: [
      ['/about/', 'About'],
      ['/pricing/', 'Pricing'],
      ['/account/', 'Your data and settings'],
    ],
  },
  {
    title: 'Policies',
    links: [
      ['/privacy/', 'Privacy policy'],
      ['/cookies/', 'Cookie policy'],
      ['/terms/', 'Terms of use'],
      ['/refunds/', 'Refund policy'],
      ['/accessibility/', 'Accessibility'],
    ],
  },
];
const year = new Date().getFullYear();
---
<footer class="site-footer on-board">
  <div class="wrap">
    <div class="top">
      <div class="about">
        <a class="brand" href={url('/')}>
          <Logo size={30} />
          <span>{site.name}</span>
        </a>
        <p class="sign">Stop memorizing graphs. Start moving them.</p>
        <p class="note">
          Free lessons for AP Microeconomics and AP Macroeconomics. No account, no ads, no tracking.
          Found a mistake? <a href={site.contactUrl}>Tell us on GitHub</a>.
        </p>
      </div>
      {groups.map((g) => (
        <nav aria-label={g.title} class="group">
          <h2 class="group-title">{g.title}</h2>
          <ul>
            {g.links.map(([href, label]) => (
              <li><a href={url(href)}>{label}</a></li>
            ))}
          </ul>
        </nav>
      ))}
    </div>
    <div class="bottom">
      <p>AP is a trademark registered by the College Board, which is not affiliated with, and does not endorse, this site.</p>
      <p>&copy; {year} <a href={site.ownerUrl}>{site.owner}</a></p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    margin-top: auto;
    padding-block: 4.5rem 2rem;
    font-size: 0.97rem;
  }
  .top {
    display: grid;
    gap: 2.5rem;
  }
  @media (min-width: 860px) {
    .top {
      grid-template-columns: 1.8fr repeat(3, 1fr);
    }
  }
  .about {
    display: grid;
    gap: 1rem;
    align-content: start;
    max-width: 38ch;
  }
  .brand {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 1.35rem;
    letter-spacing: -0.03em;
    text-decoration: none;
    justify-self: start;
  }
  .sign {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: clamp(1.6rem, 1.3rem + 1vw, 2.1rem);
    line-height: 1.05;
    letter-spacing: -0.03em;
  }
  .note {
    color: var(--ink-2);
    font-size: 0.95rem;
  }
  .group-title {
    font-family: var(--font-text);
    font-weight: 800;
    font-size: 0.78rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--sun);
    margin-bottom: 0.8rem;
  }
  .group ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 0.1rem;
  }
  .group a {
    display: inline-block;
    padding-block: 0.3rem;
    text-decoration: none;
    color: var(--ink-2);
    font-weight: 600;
  }
  .group a:hover {
    color: var(--ink);
  }
  .bottom {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 0.75rem 2rem;
    margin-top: 3.5rem;
    padding-top: 1.25rem;
    border-top: 1px solid var(--line);
    color: var(--ink-3);
    font-size: 0.87rem;
  }
  .bottom p:first-child {
    max-width: 64ch;
  }
</style>
